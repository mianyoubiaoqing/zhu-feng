# 《筑风》关卡设计说明

## 设计依据

知识库将本项目对应的主要体验归入“谜题破解、逻辑挑战、计算挑战”：玩家从疑惑开始，尝试解读规则，形成解法并验证结果（第 56–57、66 页）。知识库对新手引导提出“简单上手、步步引导深入、最终展开完整复杂”的学习曲线，同时要求避免无意义重复并规划难度梯度（第 106–107 页）。

因此，六关没有同时抛出全部规则。每一关只增加一个主要判断；已经学过的操作会在后续关卡改变用途，而不是原样重复。

## 关卡梯度

| 关卡 | 新判断 | 玩家操作 | 失败信息 | 参考解 / 精简预算 | 设计原因 |
|---|---|---|---|---|---|
| 01 转动风机 | 固定风机可以旋转 | 将左侧风机顺时针旋转一次 | 风种进入无风格 | 0 / 0 | 不放置装置，先建立“观察风场—改变朝向—测试”的最短循环。 |
| 02 第一道弯 | 导风板连接相邻方向 | 在 `(5,4)` 放置向左上连接的导风板 | 直行撞墙或转向错误 | 2 / 2 | 单个装置、单个转折，玩家只需要理解导风板端口。额外 2 金币允许试错。 |
| 03 绕开深渊 | 同一条路线需要连续规划 | 在 `(2,4)` 向上转，再在 `(2,1)` 向右转 | 风种坠入深渊 | 4 / 4 | 深渊把错误路线变成可见结果；预算要求玩家在放置前计算两次转弯。 |
| 04 风阀分流 | 两路风在交叉格发生冲突 | 在 `(4,2)` 放置朝右的单向风阀 | 风向冲突；旋走下方风机则缺少供能 | 2 / 2 | 下方风机必须保留以驱动涡轮，所以玩家不能用免费旋转规避问题，只能让风阀允许运输风通过并截断竖向风。 |
| 05 双路供能 | 运送路线与供能路线职责分离 | 旋转上方风机；在 `(7,4)` 放置导风板 | 门未供能时发生碰撞 | 2 / 2 | 玩家需要同时读取门、涡轮和两台风机，但每条风路本身仍是单一任务。墙体会在涡轮之后结束供能风，避免额外冲突。 |
| 06 风路工坊 | 三种装置承担不同职责 | 两块导风板绕坑；挡风板在涡轮后截流；单向阀隔离右侧逆风 | 坠落、冲突、碰撞、缺少供能分别指向不同错误 | 7 / 7 | 最终关把已学规则组合成一张图：路线规划、资源计算、供能条件和冲突处理必须同时成立。9 金币上限保留一次错误放置空间，7 金币给出明确精简目标。 |

## 流程选择

- 六关全部开放，便于截止日前直接测试任意机制，不设置进度门槛。
- 通关状态只在当前运行期间记录；关卡卡片显示完成标记。
- 通关后出现“下一关”，最后一关的按钮改为“完成委托”并返回关卡选择。
- 每关 HUD 同时显示目标和一句施工提示；提示只说明当前判断，不公布坐标答案。

## 验收标准

- 每关至少存在一个不超过精简预算的解。
- 参考解必须通过相同的 `GameSession`、`WindSolver` 和 `CargoSimulator`，不使用测试专用规则。
- 关卡 04 旋走供能风机会触发“必需涡轮尚未供能”，确保单向风阀不是可跳过装置。
- 关卡 06 缺少挡风板、单向风阀或任一导风板时均不能抵达终点。
